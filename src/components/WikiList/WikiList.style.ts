import css from "styled-jsx/css";

const wikiListStyles = css`
  h3 {
    text-transform: uppercase;
    margin: 0 0 8px;
    font-weight: 900;
  }

  p {
    font-size: 16px;
    font-weight: 300;
  }
`;

export default wikiListStyles;
